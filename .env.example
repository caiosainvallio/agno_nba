# Copy to `.env` (not committed) and adjust:
# cp .env.example .env

DATABASE_URL=postgresql+psycopg://agno:agno@localhost:5532/agno
GROQ_API_KEY=
# Optional override:
# GROQ_MODEL=llama-3.3-70b-versatile
